name: GitHub Pages

on:
  # Only main can publish: the github-pages environment is tied to the Pages
  # source branch, so branch pushes were rejected instantly and showed a red
  # "deploy" check on every pull request. Deploy happens on merge to main.
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Rebuild the browser bundle
        run: python3 scripts/build_data.py
      - name: Stage only the files Pages should serve
        run: |
          mkdir -p _site/assets _site/data
          cp index.html transit.html _site/
          cp assets/styles.css assets/app.js _site/assets/
          cp data/generated.js _site/data/
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deploy
        uses: actions/deploy-pages@v4
